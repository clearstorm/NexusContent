backend:
  name: git-gateway

local_backend: true

collections:
  - name: posts
    label: Posts
    folder: collections/posts
    format: json
    fields:
      - { name: title, label: Title, widget: string }
      - { name: body, label: Body, widget: markdown }
