# Fixture content repository

This directory simulates a content repository that has been edited by a Git based
CMS. It intentionally contains non-content files (CMS configuration, images,
documentation) alongside NexusContent content to prove that the Git provider only
reads convention based content locations.
