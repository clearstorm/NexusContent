{
  "name": "@nexuscontent/example-astro-wordpress",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview",
    "astro": "astro",
    "push:project-contract": "node --env-file-if-exists=.env ../../scripts/nexus-contract.mjs push --schema src/schema/schema.ts",
    "sections:contract": "node --env-file-if-exists=.env ../../scripts/nexus-contract.mjs generate --schema src/schema/schema.ts --custom sections.custom.json"
  },
  "dependencies": {
    "@nexuscontent/core": "file:../.."
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.2",
    "typescript": "^6.0.0"
  }
}
